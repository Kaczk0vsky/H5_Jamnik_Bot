# H5_Jamnik_Bot
Docker environment for discord bot Jamnik

### Installation
1. sudo apt update
2. sudo apt install -y docker.io docker-compose
3. sudo docker-compose build
4. sudo docker-compose up -d
5. sudo docker ps - see runing cointainers
6. sudo docker-compose logs -f - logs